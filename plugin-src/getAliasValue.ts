// import { Token } from "./code";

// export function getAliasValue(alias: string, tokens: Token[]) {
//   if (!alias) return false;
//   const token = tokens.find(token => token.name === alias)
//   if (!token) return false;
//   return token.value
// }